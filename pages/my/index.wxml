<scroll-view
    class="my"
    scroll-y="{{true}}"
    enhanced="{{true}}"
    enable-flex="{{true}}"
    refresher-background="#ffeadb"
    refresher-triggered="{{refresherTriggered}}"
    refresher-enabled="{{true}}"
    bindrefresherrefresh="onRefresh"
    show-scrollbar="{{false}}">
        <view class="detail" style="padding-top: {{headerHight}}px">
            <block wx:if="{{!username || !photo}}">
                <view class="person">
                    <image src="/asserts/temp/photo.png" class="person_img"/>
                    <view class="person_name">点击登录</view>
                    <view class="person_detail"></view>
                    <button class="user_btn" open-type="getUserInfo" bindtap="getUserProfile">
                        按钮文本
                    </button>
                </view>
            </block>
            <block wx:else>
                <view class="person">
                    <image src="{{photo}}" class="person_img"/>
                    <view class="person_name">{{username}}</view>
                    <view class="person_detail"></view>
                </view>
            </block>
        </view>
        <view class="setting">
            <listItem iconName="order" text="账单明细" isFirst />
            <listItem iconName="team" text="组团记录" />
            <listItem iconName="class" text="报名课程" isLast />
        </view>
        <view class="setting">
            <listItem iconName="service" text="联系客服" isFirst />
            <listItem iconName="our" text="关于我们" isLast />
        </view>
</scroll-view>