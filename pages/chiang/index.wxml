<view class="wrap">
    <view class="top">
        <header text="我的"/>
        <tab list="{{ typeList }}" activeIdx="{{selectTypeIdx}}" bindOnTapTab="onTapType"/>
        <!-- <scroll-view scroll-x class="tab" enhanced="{{true}}" enable-flex="true">
            <block wx:for="{{tab}}" wx:key="id">
                <view bind:tap="onTapTab" data-idx="{{index}}" class="tab_item {{activeTabIdx === index ? 'tab_item_active' : ''}}">
                    {{item.name}}
                    <block wx:if="{{ activeTabIdx === index }}">
                        <view class="tab_item_border"></view>
                    </block>
                </view>
            </block>
        </scroll-view> -->
        <!-- <view class="tab">
            <van-tabs line-width="{{25}}" ellipsis="{{false}}" active="{{ active }}" sticky="{{ true }}" bind:change="onChange">
                <block wx:for="{{tab}}" wx:key="id">
                    <van-tab title="{{item.name}}" />
                </block>
            </van-tabs>
        </view> -->
    </view>
     <scroll-view 
        scroll-y="{{true}}" 
        class="content" 
        enhanced="{{true}}" 
        show-scrollbar="{{false}}">
        <swiper class="swiper" indicator-dots="{{true}}" autoplay="{{true}}" interval="{{3000}}" duration="{{300}}" circular="{{true}}">
            <block wx:for="{{swiperList}}" wx:key="id">
                <swiper-item class="swiper_item">
                    <image class="swiper_img" src="{{item.url}}"/>
                </swiper-item>
            </block>
        </swiper>
        <view class="class">
            <tab list="{{projectList}}" activeIdx="{{selectProjectIdx}}" bindOnTapTab="onTapClassTab"/>
        </view>
        <block wx:for="{{subjectList}}" wx:key="index">
            <view class="chunk" >
                <subject-item subject="{{item}}" data-detail="{{item}}" bindOnTap="onTapSubjectItem"/>
            </view>
        </block>
        <btm-tip />
        <phone-component visible="{{phoneDialogVisible}}" bindOnClose="onClosePhoneDialog"/>
    </scroll-view>
</view>